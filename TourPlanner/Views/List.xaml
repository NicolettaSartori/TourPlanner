<UserControl x:Class="TourPlanner.Views.List"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:TourPlanner.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="200">
    
    <UserControl.DataContext>
        <vm:ListViewModel />
    </UserControl.DataContext>

    <Border Margin="8" BorderBrush="LightGray" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
         
            <ListBox Grid.Row="0" BorderThickness="0" ItemsSource="{Binding Tours}">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition/>
                            </Grid.RowDefinitions>

                            <TextBlock Text="{Binding Name}" Padding="5" Grid.Row="0"/>
                            <Border Height="1" Background="LightGray" Grid.Row="1"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
            
            <Border Grid.Row="1" BorderBrush="LightGray" BorderThickness="0 1 0 0">
                <TextBlock Text="Vestibulum at eros" HorizontalAlignment="Center" Padding="5" Foreground="Black"/>
            </Border>
        </Grid>
    </Border>
</UserControl>